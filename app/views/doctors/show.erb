<% @doctor.appointments.each do |appointment| %>
  <%= appointment.doctor.name %>
  <%= link_to appointment.patient.name, patient_path(appointment.patient.id) %>
  <%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
<% end %>
